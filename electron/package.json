{
  "name": "lazycreatives-uploader",
  "productName": "LazyCreatives Uploader",
  "version": "0.1.0",
  "description": "LazyCreatives Uploader — automatically publish your finished mixes to SoundCloud",
  "main": "electron/main.js",
  "scripts": {
    "dev:vite": "vite",
    "dev:electron": "wait-on tcp:5173 && cross-env LAZYUP_DEV=1 LAZYUP_MOCK=1 node scripts/run-electron.js",
    "start": "concurrently -k -n VITE,ELECTRON -c blue,green \"npm:dev:vite\" \"npm:dev:electron\"",
    "build": "vite build",
    "build:py": "cd ../backend && .venv/Scripts/pyinstaller sidecar.spec --noconfirm --distpath dist",
    "dist": "npm run build && npm run build:py && electron-builder",
    "test": "vitest run"
  },
  "build": {
    "appId": "com.lazycreatives.uploader",
    "productName": "LazyCreatives Uploader",
    "directories": { "output": "release", "buildResources": "build" },
    "files": ["dist/**/*", "electron/**/*", "package.json"],
    "extraResources": [
      { "from": "../backend/dist/lazyupload-sidecar", "to": "sidecar" }
    ],
    "mac": {
      "category": "public.app-category.music",
      "target": ["dmg"],
      "icon": "build/icon.png",
      "hardenedRuntime": true,
      "gatekeeperAssess": false
    },
    "dmg": { "title": "LazyCreatives Uploader" },
    "win": { "target": ["nsis"], "icon": "build/icon.png" },
    "nsis": { "oneClick": false, "perMachine": false }
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "concurrently": "^9.0.1",
    "cross-env": "^7.0.3",
    "electron": "^31.3.1",
    "electron-builder": "^24.13.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vitest": "^2.0.5",
    "jsdom": "^25.0.0",
    "wait-on": "^8.0.0",
    "@types/react": "^18.3.4",
    "@types/react-dom": "^18.3.0"
  }
}
